{
  "name": "@radiant/bun",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "bun run build",
    "dev": "bun run dev"
  },
  "dependencies": {
    "@radiant/core": "workspace:*"
  }
}
